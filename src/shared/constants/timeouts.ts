/** Default timeout for Copilot API calls (10 minutes). */
export const COPILOT_TIMEOUT_MS = 600_000;

/** Task type marker embedded in prompts to identify work-item summary jobs. */
export const TASK_TYPE_WORK_ITEMS_SUMMARY = '[TASK_TYPE:WORK_ITEMS_SUMMARY]';
